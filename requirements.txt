# Web framework
fastapi==0.136.3
starlette==1.2.1  # pin explicitly — fastapi allows >=0.46.0, fix CVE PYSEC-2026-161 (fixed in 1.0.1)
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Auth
# Replaced python-jose (CVE: PYSEC-2024-232/233, conflicts with pyasn1>=0.6)
# PyJWT is the actively maintained successor with no pyasn1 dependency
PyJWT[crypto]==2.13.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Validation & settings
pydantic[email]==2.10.3
pydantic-settings==2.6.1

# HTTP client (CoinGecko)
httpx==0.28.1

# Cache
redis[asyncio]==5.2.1

# Rate limiting
slowapi==0.1.9

# AI
anthropic==0.40.0

# gRPC
grpcio==1.68.1
grpcio-tools==1.68.1
grpcio-reflection==1.68.1
googleapis-common-protos==1.65.0

# Observability
prometheus-fastapi-instrumentator==8.0.0  # v8+ requires starlette>=1.0.0, resolving CVE conflict
prometheus-client==0.21.1

# Utils
python-dotenv==1.2.2
python-multipart==0.0.27

# Dev / CI
ruff==0.8.4
mypy==1.13.0
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==6.0.0
pip-audit==2.7.3
